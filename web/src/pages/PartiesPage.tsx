import { FormEvent, useEffect, useState } from "react";

import { api } from "../api";
import { fail, type Party, type PartyRole, PageHeader } from "../appData";
import { AppShell } from "../components/AppShell";

const emptyParty = () => ({ name: "", role: "customer" as PartyRole, email: "", phone: "", notes: "" });

export default function PartiesPage() {
  const [parties, setParties] = useState<Party[]>([]);
  const [party, setParty] = useState(emptyParty);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [message, setMessage] = useState("");
  const [busy, setBusy] = useState(false);
  async function load() { setParties((await api<{ parties: Party[] }>("/api/parties")).parties); }
  useEffect(() => { load().catch((error) => setMessage(fail(error, "Unable to load parties."))); }, []);
  function reset() { setEditingId(null); setParty(emptyParty()); }
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { await api(editingId ? `/api/parties/${editingId}` : "/api/parties", { method: editingId ? "PATCH" : "POST", body: JSON.stringify(party) }); const editing = Boolean(editingId); reset(); await load(); setMessage(editing ? "Party updated." : "Party registered."); } catch (error) { setMessage(fail(error, "Unable to save party.")); } finally { setBusy(false); } }
  async function remove(item: Party) { if (!window.confirm("Delete this party? Existing transactions will keep its name.")) return; try { await api(`/api/parties/${item.id}`, { method: "DELETE" }); await load(); setMessage("Party deleted. Historical transactions were preserved."); } catch (error) { setMessage(fail(error, "Unable to delete party.")); } }
  return <AppShell activePage="parties"><main className="app-main" id="app-main"><PageHeader kicker="Directory" title="Customers & suppliers" /><section className="parties-card dedicated-card"><div className="transactions-heading"><div><p className="app-kicker">Business contacts</p><h2>Party directory</h2></div><p>Register the people and businesses you transact with once, then attach them to payments and expenses.</p></div><div className="parties-layout"><form className="party-form" onSubmit={submit}><div className="transaction-fields"><label className="transaction-field"><span>Name</span><input maxLength={120} autoComplete="organization" required value={party.name} onChange={(e) => setParty({ ...party, name: e.target.value })} disabled={busy} /></label><label className="transaction-field"><span>Role</span><select value={party.role} onChange={(e) => setParty({ ...party, role: e.target.value as PartyRole })} disabled={busy}><option value="customer">Customer</option><option value="supplier">Supplier</option><option value="both">Customer & supplier</option></select></label></div><div className="transaction-fields"><label className="transaction-field"><span>Email (optional)</span><input type="email" maxLength={254} value={party.email} onChange={(e) => setParty({ ...party, email: e.target.value })} disabled={busy} /></label><label className="transaction-field"><span>Phone (optional)</span><input type="tel" maxLength={40} value={party.phone} onChange={(e) => setParty({ ...party, phone: e.target.value })} disabled={busy} /></label></div><label className="transaction-field"><span>Notes (optional)</span><textarea maxLength={500} rows={3} value={party.notes} onChange={(e) => setParty({ ...party, notes: e.target.value })} disabled={busy} /></label><button className="button button-primary" type="submit" disabled={busy}>{editingId ? "Save party" : "Register party"}</button>{editingId && <button className="button button-secondary" type="button" onClick={reset}>Cancel edit</button>}<p className="transaction-message" role="status">{message}</p></form><div className="party-directory"><div className="section-row"><h2>Registered</h2><span>{parties.length} {parties.length === 1 ? "party" : "parties"}</span></div>{parties.length === 0 ? <p className="transaction-empty">No customers or suppliers registered yet.</p> : parties.map((item) => <div className="party-row" key={item.id}><div><strong>{item.name}</strong><small>{[item.email, item.phone].filter(Boolean).join(" · ") || "No contact details"}</small></div><span className="party-role">{item.role === "both" ? "Customer & supplier" : item.role}</span><div className="transaction-actions"><button type="button" onClick={() => { setEditingId(item.id); setParty({ name: item.name, role: item.role, email: item.email || "", phone: item.phone || "", notes: item.notes || "" }); }}>Edit</button><button className="transaction-delete" type="button" aria-label={`Delete ${item.name}`} onClick={() => remove(item)}>×</button></div></div>)}</div></div></section></main></AppShell>;
}
