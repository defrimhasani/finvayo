import { useEffect, useState } from "react";
import { api } from "../api";
import { daysBetween, fail, type Financials, type Overview, PageHeader, UpcomingLedger } from "../appData";
import { AppShell } from "../components/AppShell";
import { money } from "../utils";

function chartPaths(overview: Overview) { const values = [...overview.points.map((p) => p.balanceMinor), overview.protectedMinor, 0]; const min = Math.min(...values); const max = Math.max(...values); const pad = Math.max((max - min) * .12, 100); const chartMin = min - pad; const chartMax = max + pad; const range = chartMax - chartMin || 1; const x = (date: string) => daysBetween(date, overview.horizonStart) / 90 * 900; const y = (value: number) => 250 - (value - chartMin) / range * 250; let line = `M0 ${y(overview.currentCashMinor).toFixed(1)}`; for (const p of overview.points.slice(1)) line += ` H${x(p.date).toFixed(1)} V${y(p.balanceMinor).toFixed(1)}`; line += " H900"; let reserve = `M0 ${y(overview.points[0]?.protectedMinor ?? overview.protectedMinor).toFixed(1)}`; for (const p of overview.points.slice(1)) reserve += ` H${x(p.date).toFixed(1)} V${y(p.protectedMinor).toFixed(1)}`; return { line, area: `${line} V250 H0 Z`, reserve: `${reserve} H900`, ticks: [chartMax, chartMax-range/3, chartMax-2*range/3, chartMin] }; }
export default function CashPlanPage() { const [overview, setOverview] = useState<Overview | null>(null); const [currency, setCurrency] = useState("USD"); const [message, setMessage] = useState(""); useEffect(() => { api<Financials>("/api/financials").then((r) => { setOverview(r.overview); setCurrency(r.currency || "USD"); }).catch((e) => setMessage(fail(e, "Unable to load cash plan."))); }, []); const chart = overview ? chartPaths(overview) : null; const shortMoney = (value: number) => new Intl.NumberFormat(undefined, { style: "currency", currency, notation: "compact", maximumFractionDigits: 1 }).format(value / 100); return <AppShell activePage="cash-plan"><main className="app-main" id="app-main"><PageHeader kicker="Forecast" title="Cash plan" /><section className="outlook-card dedicated-card"><div className="outlook-heading"><div><p className="app-kicker">Cash outlook</p><h2>The next 90 days</h2></div><div className="legend"><span><i />Projected cash</span><span><i className="dashed" />Protected level</span></div></div>{overview && chart ? <><div className="app-chart" role="img" aria-label={`Projected cash reaches a low of ${money(overview.lowestBalanceMinor, currency)}.`}><div className="axis">{chart.ticks.map((tick, i) => <span key={i}>{shortMoney(tick)}</span>)}</div><div className="plot"><svg viewBox="0 0 900 250" preserveAspectRatio="none" aria-hidden="true"><path className="app-area" d={chart.area} /><path className="app-line" d={chart.line} /><path className="reserve-line" d={chart.reserve} /></svg><div className="plot-dates"><span>Today</span><span>30 days</span><span>60 days</span><span>90 days</span></div></div></div><p className="chart-summary"><span>01</span>Your lowest projected balance is <strong>{money(overview.lowestBalanceMinor, currency)} {overview.limitingDate.startsWith("in ") ? overview.limitingDate : `on ${overview.limitingDate}`}</strong>, {money(Math.abs(overview.lowestHeadroomMinor), currency)} {overview.lowestHeadroomMinor >= 0 ? "above" : "below"} your protected level.</p></> : <p className="chart-summary"><span>01</span>{message || "Add a confirmed balance to start your 90-day projection."}</p>}</section><section className="cash-plan-ledger"><UpcomingLedger overview={overview} currency={currency} /></section></main></AppShell>; }
