import { FormEvent, useEffect, useState } from "react";

import { api } from "../api";
import { categories, type CashEntry, type Direction, emptyTransaction, fail, type Financials, type Party, PageHeader, type Timing, transactionLabel, validIsoDate, validPositiveAmount } from "../appData";
import { AppShell } from "../components/AppShell";
import { money } from "../utils";

function initialTransaction() {
  const value = emptyTransaction();
  const params = new URLSearchParams(window.location.search);
  const amount = params.get("amount");
  const date = params.get("date");
  const name = params.get("name");
  const category = params.get("category");
  if (amount && date && validPositiveAmount(amount) && validIsoDate(date)) {
    value.direction = "outflow"; value.timing = "planned"; value.amount = amount; value.date = date; value.status = "planned";
    if (name && name.length <= 120) value.name = name;
    if (category && categories.outflow.some(([key]) => key === category)) value.category = category;
    else value.category = "equipment";
  }
  return value;
}

export default function TransactionsPage() {
  const [entries, setEntries] = useState<CashEntry[]>([]);
  const [parties, setParties] = useState<Party[]>([]);
  const [currency, setCurrency] = useState("USD");
  const [transaction, setTransaction] = useState(initialTransaction);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [message, setMessage] = useState("");
  const [busy, setBusy] = useState(false);
  async function load() { const [financials, directory] = await Promise.all([api<Financials>("/api/financials"), api<{ parties: Party[] }>("/api/parties")]); setEntries(financials.entries); setCurrency(financials.currency || "USD"); setParties(directory.parties); }
  useEffect(() => { load().catch((error) => setMessage(fail(error, "Unable to load transactions."))); }, []);
  function reset() { setEditingId(null); setTransaction(emptyTransaction()); }
  async function submit(event: FormEvent) { event.preventDefault(); if (!validPositiveAmount(transaction.amount)) return setMessage("Enter a valid positive amount with up to two decimal places."); const existing = entries.find((entry) => entry.id === editingId); const recorded = transaction.timing === "recorded"; const amountMinor = Math.round(Number(transaction.amount) * 100); setBusy(true); try { await api(editingId ? `/api/cash-entries/${editingId}` : "/api/cash-entries", { method: editingId ? "PATCH" : "POST", body: JSON.stringify({ direction: transaction.direction, name: transaction.name, amountMinor: existing?.status === "paid" ? existing.amountMinor : amountMinor, actualAmountMinor: recorded ? amountMinor : undefined, actualDate: recorded ? transaction.date : undefined, scheduledDate: existing?.status === "paid" ? existing.scheduledDate : transaction.date, status: recorded ? "paid" : transaction.status, partyId: transaction.partyId || (editingId ? null : undefined), invoiceReference: transaction.direction === "inflow" ? transaction.invoiceReference : undefined, category: transaction.category, recurrence: !recorded && transaction.recurring ? "monthly" : editingId ? null : undefined }) }); const editing = Boolean(editingId); reset(); await load(); setMessage(editing ? "Transaction updated." : "Transaction added."); } catch (error) { setMessage(fail(error, "Unable to record transaction.")); } finally { setBusy(false); } }
  function edit(entry: CashEntry) { setEditingId(entry.id); setTransaction({ direction: entry.direction, timing: entry.status === "paid" ? "recorded" : "planned", name: entry.name, amount: (entry.amountMinor / 100).toFixed(2), date: entry.scheduledDate, partyId: entry.partyId || "", invoiceReference: entry.invoiceReference || "", category: entry.category || (entry.direction === "inflow" ? "service_income" : "other"), status: entry.status === "paid" ? "expected" : entry.storedStatus || entry.status, recurring: entry.recurrence === "monthly" }); document.querySelector("#transaction-form")?.scrollIntoView({ behavior: "smooth", block: "center" }); }
  async function patchEntry(id: string, payload: object, success = "") { try { await api(`/api/cash-entries/${id}`, { method: "PATCH", body: JSON.stringify(payload) }); await load(); if (success) setMessage(success); } catch (error) { setMessage(fail(error, "Unable to update transaction.")); } }
  async function markPaid(entry: CashEntry) { const amount = window.prompt("Actual amount", ((entry.actualAmountMinor || entry.amountMinor) / 100).toFixed(2)); const actualDate = window.prompt("Actual date (YYYY-MM-DD)", new Date().toISOString().slice(0, 10)); if (amount === null || actualDate === null || !validPositiveAmount(amount) || !validIsoDate(actualDate)) return; await patchEntry(entry.id, { status: "paid", actualAmountMinor: Math.round(Number(amount) * 100), actualDate }, "Transaction marked paid."); }
  async function remove(entry: CashEntry) { if (!window.confirm("Delete this transaction?")) return; try { await api(`/api/cash-entries/${entry.id}`, { method: "DELETE" }); await load(); setMessage("Transaction deleted."); } catch (error) { setMessage(fail(error, "Unable to delete transaction.")); } }
  const allowedParties = parties.filter((item) => item.role === (transaction.direction === "inflow" ? "customer" : "supplier") || item.role === "both");
  const sorted = [...entries].sort((a, b) => b.scheduledDate.localeCompare(a.scheduledDate));
  return <AppShell activePage="transactions"><main className="app-main" id="app-main"><PageHeader kicker="Money movement" title="Transactions" /><section className="transactions-card dedicated-card"><div className="transactions-heading"><div><p className="app-kicker">Cash entries</p><h2>Record a transaction</h2></div><p>Log completed payments and expenses, or add expected movements to your cash plan.</p></div><div className="transactions-layout"><form className="transaction-form" id="transaction-form" onSubmit={submit}><fieldset className="transaction-kind" disabled={busy}><legend>Transaction type</legend>{(["inflow", "outflow"] as Direction[]).map((value) => <label key={value}><input type="radio" checked={transaction.direction === value} onChange={() => setTransaction({ ...transaction, direction: value, category: categories[value][0][0], partyId: "", status: value === "inflow" ? "expected" : "planned" })} /><span>{value === "inflow" ? "Payment received" : "Expense paid"}</span></label>)}</fieldset><fieldset className="transaction-kind" disabled={busy}><legend>Timing</legend>{(["recorded", "planned"] as Timing[]).map((value) => <label key={value}><input type="radio" checked={transaction.timing === value} onChange={() => setTransaction({ ...transaction, timing: value })} /><span>{value === "recorded" ? "Already happened" : "Expected / planned"}</span></label>)}</fieldset><label className="transaction-field"><span>Description</span><input maxLength={120} required value={transaction.name} onChange={(e) => setTransaction({ ...transaction, name: e.target.value })} disabled={busy} /></label><div className="transaction-fields"><label className="transaction-field"><span>Amount</span><input inputMode="decimal" required value={transaction.amount} onChange={(e) => setTransaction({ ...transaction, amount: e.target.value })} disabled={busy} /></label><label className="transaction-field"><span>Date</span><input type="date" required value={transaction.date} onChange={(e) => setTransaction({ ...transaction, date: e.target.value })} disabled={busy} /></label></div><label className="transaction-field"><span>{transaction.direction === "inflow" ? "Customer" : "Supplier"}</span><select value={transaction.partyId} onChange={(e) => setTransaction({ ...transaction, partyId: e.target.value })} disabled={busy}><option value="">No registered party</option>{allowedParties.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label>{transaction.direction === "inflow" && <label className="transaction-field"><span>Invoice reference</span><input maxLength={80} value={transaction.invoiceReference} onChange={(e) => setTransaction({ ...transaction, invoiceReference: e.target.value })} disabled={busy} /></label>}<label className="transaction-field"><span>Category</span><select value={transaction.category} onChange={(e) => setTransaction({ ...transaction, category: e.target.value })} disabled={busy}>{categories[transaction.direction].map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label>{transaction.timing === "planned" && <><label className="transaction-field"><span>Status</span><select value={transaction.status} onChange={(e) => setTransaction({ ...transaction, status: e.target.value })} disabled={busy}>{(transaction.direction === "inflow" ? [["expected", "Expected"], ["invoiced", "Invoiced"], ["unlikely", "Unlikely"]] : [["planned", "Planned"]]).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="terms-check"><input type="checkbox" checked={transaction.recurring} onChange={(e) => setTransaction({ ...transaction, recurring: e.target.checked })} disabled={busy} /><span>Repeat monthly in the 90-day outlook</span></label></>}<button className="button button-primary" type="submit" disabled={busy}>{editingId ? "Save transaction" : "Add transaction"}</button>{editingId && <button className="button button-secondary" type="button" onClick={reset}>Cancel edit</button>}<p className="transaction-message" role="status">{message}</p></form><div className="transaction-history"><div className="section-row"><h2>Payments & expenses</h2><span>{entries.length} entries</span></div>{sorted.length === 0 ? <p className="transaction-empty">No payments or expenses added yet.</p> : sorted.map((entry) => <div className={`transaction-row ${entry.direction}`} key={entry.id}><div><strong>{entry.name}</strong><small>{entry.scheduledDate} · {entry.status} · {transactionLabel(entry)}</small></div><b>{entry.direction === "inflow" ? "+" : "−"}{money(entry.actualAmountMinor || entry.amountMinor, currency)}</b><div className="transaction-actions">{entry.status !== "paid" && <><button type="button" onClick={() => markPaid(entry)}>Mark paid</button><button type="button" onClick={() => patchEntry(entry.id, { included: !entry.included })}>{entry.included ? "Exclude" : "Include"}</button></>}<button type="button" onClick={() => edit(entry)}>Edit</button><button className="transaction-delete" type="button" aria-label={`Delete ${entry.name}`} onClick={() => remove(entry)}>×</button></div></div>)}</div></div></section></main></AppShell>;
}
